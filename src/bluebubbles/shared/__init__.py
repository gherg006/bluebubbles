"""Protocol-safe functionality shared by the client and server.

This package is intentionally independent of both application implementations.
It exposes validated network contracts, stable identifiers, and deterministic
serialisation, but contains no business, persistence, GUI, or private-key logic.
"""
